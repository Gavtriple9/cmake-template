cmake_minimum_required(VERSION 3.14...3.22)
project(${PROJ_NAME_CAPITAL}_Tests LANGUAGES CXX)

add_subdirectory(lib/googletest)

include_directories(${${PROJ_NAME_UPPER}_HEADERS_DIR})
include_directories(lib/googletest/googletest/include)

set(SOURCES main.cpp src/${PROJ_NAME_LOWER}_tests.cpp)

add_executable(${PROJ_NAME_LOWER}_tests ${SOURCES})
target_link_libraries(${PROJ_NAME_LOWER}_tests ${PROJ_NAME_LOWER} gtest)
install(TARGETS ${PROJ_NAME_LOWER}_tests DESTINATION bin)

